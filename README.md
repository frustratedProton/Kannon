# Kannon

This project is a Proof of Concept and an educational recreation of `htop` to understand linux internals. It parses the `/proc` filesystem directly without relying on external libraries like `psutil`.